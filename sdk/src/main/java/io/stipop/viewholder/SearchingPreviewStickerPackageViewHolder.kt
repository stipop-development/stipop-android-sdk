package io.stipop.viewholder

import io.stipop.Utils
import io.stipop.custom.RecyclerDecoration

class SearchingPreviewStickerPackageViewHolder {
    val decoration = RecyclerDecoration(Utils.dpToPx(7F).toInt())
}